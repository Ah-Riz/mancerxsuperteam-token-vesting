# apps/web

Frontend dApp — **owner: @Ah-Riz / Geral**.

Scaffold pending. Geral picks the stack (Vite + React, Next.js, etc.) and pushes the initial commit here.

Once a `package.json` exists, this workspace is picked up automatically by `pnpm install` at the repo root (see `pnpm-workspace.yaml`).

{
  "name": "@mancer-vesting/ts-client",
  "version": "0.0.0",
  "private": true,
  "description": "Off-chain Merkle tooling — leaf encoder, tree builder, proof generator",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
